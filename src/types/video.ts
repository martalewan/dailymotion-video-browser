export interface Video {
    id: string;
    title: string;
    thumbnail_360_url: string;
}

export interface VideoDetails {
    id: string;
    title: string;
    description: string;
    thumbnail_720_url: string;
}